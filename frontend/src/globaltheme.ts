import { createTheme } from '@mui/material';

const globalTheme = createTheme({
    palette: {
        secondary: {
            main: '#FEF32F',
        },
        info: {
            main: '#f3f5f7',
        },
    },
});

export default globalTheme;
